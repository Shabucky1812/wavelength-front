import React from 'react'

const ProfileSearch = ({message}) => {
  return (
    <div>
      <p>Search bar</p>
      <p>profile preview list</p>
      {message}
    </div>
  )
}

export default ProfileSearch